# Bible API Docker Configuration
# Copy this file to .env and customize as needed

# Database Configuration
# Database path inside the container (should match the mounted file)
BIBLE_DB_PATH=/app/db/bible.db

# Note: The bible.db file must exist in the project root directory.
# It will be automatically mounted into the container.

# API Generation Configuration
# Path where API files will be generated
API_OUTPUT_PATH=/app/api

# Whether to overwrite existing API files
# Set to false to skip generation if files already exist (faster startup)
API_OVERWRITE=false

# Whether to pretty-print JSON files
API_PRETTY_PRINT=true

# Node.js Environment
NODE_ENV=production

# Node.js Memory Limit (in MB)
# Increase this if you get "heap out of memory" errors with large databases
# NODE_OPTIONS=--max-old-space-size=4096

# Nginx Configuration
# Ports to expose nginx on the host
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# SSL Certificate Configuration
# Path to SSL certificates (relative to project root)
# For Let's Encrypt: ./ssl/cert.pem and ./ssl/key.pem
# For self-signed: generate using the script in the ssl directory
SSL_CERT_PATH=./ssl/cert.pem
SSL_KEY_PATH=./ssl/key.pem

# CORS Configuration
# Comma-separated list of allowed origins (use * for all)
CORS_ALLOW_ORIGIN=*

# Optional: Specific translations to generate (comma-separated)
# Leave empty to generate all translations
# API_TRANSLATIONS=

# Optional: Batch size for processing translations
# Lower values use less memory but take longer
# Recommended: 10-25 for large databases, 50+ for smaller ones
# API_BATCH_SIZE=10

# AWS Configuration (for S3 uploads - optional)
# AWS_PROFILE=aolabs
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key
